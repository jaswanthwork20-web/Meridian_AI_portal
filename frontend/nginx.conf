server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index login.html;

    location / {
        try_files $uri $uri/ /login.html;
    }

    location = /health {
        add_header Content-Type text/plain;
        return 200 'ok';
    }
}
